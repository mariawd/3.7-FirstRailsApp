<article class="container min-height">
<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name, :requiered => true %>
    </div>

    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description, :requiered => true %>
    </div>

    <div class="field">
      <%= f.label :image_url %><br>
      <%= f.text_field :image_url, id: :product_image_url %>
    </div>

    <div class="field">
      <%= f.label :colour %><br>
      <%= f.text_field :colour, :requiered => true %>
    </div>

    <div class="field">
      <%= f.label :price %><br>
      <%= f.text_field :price, :requiered => true %>
    </div>

    <br>

    <div class="actions">
      <%= f.submit %>
    </div>

  <% end %>

</article>
